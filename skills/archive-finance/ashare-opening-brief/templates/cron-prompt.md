你正在执行 09:00 A 股开盘前简报的轻量发送任务。

规则：
1. 优先使用脚本输出；不要重新做重型新闻搜索。
2. 脚本已经整合 Google News、TrendRadar 本地输出、前一日复盘、持仓/旧候选异动、持仓/候选分析结果与新的候选股筛选。
3. 从脚本 stdout 中读取 `note_path`，再读取对应的 `opening-brief.md`。
4. 若文件存在且非空，直接把全文发送到当前会话（保持原有 Markdown 结构）。
5. 输出里必须保留 `今日执行计划表（可下单版）`，并明确包含这些字段：`计划仓位比例`、`首笔试仓比例`、`二次加仓条件`、`开盘前 5 分钟动作`、`开盘后 15 分钟动作`。
6. 对每只重点持仓/候选，必须写清楚：`高开应对`、`平开应对`、`低开应对`，以及对应的`明确买入区`、`明确卖出区`、`止损位`。
7. 还必须补充：`竞价阈值`、`开盘3分钟动作`、`开盘5分钟动作`、`开盘15分钟动作`、`第一次卖出比例`、`第二次卖出比例`、`撤单/取消条件`。
8. 决策树字段必须进一步细化为：`开盘情景分级`（至少覆盖高开0~2%、高开2~4%、高开4%以上、平开、低开0~2%、低开2%以上）与`买点类型分层`（回踩买、突破买、弱转强买、只观察不买）。
9. 还必须新增 `今日市场环境与总策略`：明确判断当天是主升、修复、轮动、退潮或其他不利环境，并直接回答：今天是否应该买入、是否应该卖出、是空仓等待、轻仓试错、还是可积极出手；同时给出建议最大总仓位。
10. 必须补充仓位档位规则：退潮/轮动/修复/主升/极强共振日分别对应什么总仓位；还要明确写出 `从空仓到轻仓`、`从轻仓到半仓`、`即使看好也不能出手`、`满仓/重仓保护`。
11. 候选股必须体现“根据新闻 + 前一日候选股 + 前一日盘面/复盘重新筛选后的最新候选股”，不要只是重复旧名单。
12. 若文件缺失或为空，只返回简短失败说明，并附上脚本输出中的关键信息。
13. 不要擅自扩写成长文；目标是在 09:00 准时交付，但执行模板必须足够让用户直接映射到短线下单动作。
